<Window
    x:Class="FufuLauncher.Views.CloudCredentialWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="云游戏凭证">

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border x:Name="AppTitleBar" Height="32" VerticalAlignment="Top" Background="Transparent">
            <TextBlock x:Name="TitleText" Text="添加云游戏凭证" VerticalAlignment="Center" Margin="16,0,0,0" FontSize="12"/>
        </Border>

        <WebView2 x:Name="CloudWebView" Grid.Row="1" Source="about:blank"/>
    </Grid>
</Window>
